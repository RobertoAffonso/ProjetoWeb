{% extends 'mainTheme.html.twig' %}

{% block title %}Atender Chamado{% endblock %}

{% block body %}
    <!-- Content Wrapper. Contains page content -->

    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>Devolver Chamado</h1>
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header"></h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-6">
                            {{ form_start(devChamado) }}
                            {{ form_row(devChamado.Nome) }}
                            {{ form_row(devChamado.Descricao) }}
                            {{ form_row(devChamado.Setor) }}
                            {{ form_row(devChamado.Prioridade) }}
                            {{ form_row(devChamado.Fila) }}
                            {{ form_row(devChamado.dataLimite) }}
                            {{ form_row(devChamado.comentario) }}
                            <p>
                            <p>
                                <button type="submit" class="btn btn-default" formnovalidate>Devolver Chamado</button>
                                {{ form_end(devChamado) }}
                        </div>
                        <!-- /.col-lg-6 (nested) -->
                    </div>
                    <!-- /.row (nested) -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
    </section>
{% endblock %}